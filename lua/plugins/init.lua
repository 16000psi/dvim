-- Returns a conf for lazy, here included are some very standard plugins
-- that do not require any special configuration
return {
	{ "folke/neoconf.nvim", cmd = "Neoconf" },
  {"folke/neodev.nvim", lazy = false},
}
